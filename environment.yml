name: pilot-development

channels:
  - diracgrid
  - conda-forge
  - defaults

dependencies:
  # runtime
  - python =3.9
  - pip
  - requests
  # testing and development
  - pycodestyle
  - caniusepython3
  - coverage
  - mock
  - pylint
  - pytest
  - pytest-mock
  - shellcheck
