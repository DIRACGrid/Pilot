name: pilot-development

channels:
  - diracgrid
  - conda-forge
  - defaults

dependencies:
  # runtime
  - python =2.7
  - pip
  - requests
  # testing and development
  - autopep8
  - caniusepython3
  - coverage
  - mock
  - pylint
  - pytest >=3.6
  - pytest-mock
  - shellcheck
